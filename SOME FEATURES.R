if(!require('googledrive')) install.packages('googledrive')

library(googledrive)
drive_auth()
drive_find(n_max = 10)

#drive_find(pattern = "/2022 - 2/*.zip", n_max = 100)

#drive_ls(path = '/2022 - 2/', pattern = '*.*')


  
  comilla simple <- alt + 39 <- ''
  comillas invertidas <- alt + 96 <- ``
  
  alt <- para seleccionar los paneles del menu superior
  
  
  MAS ATAJOS:
  
  restart R -> ctrl + shift + f10
  
  # annotater:
  
  library(janitor) # Simple Tools for Examining and Cleaning Dirty Data, CRAN v2.1.0
  library(tidyverse) # Easily Install and Load the 'Tidyverse', CRAN v1.3.2
  
  
  instalar -> datapasta
  
  atajo para escribir en varias lineas: ctrl + alt + (flechas hacia abajo o arriba, segun tu uso)
  
  
  
  # Panel de atajos:
  tools |> Keyboard Shortcuts Help <- alt + shift + k
  
  # insertar 'pipe':
  ctrl + shift + m <- `|>` 
  
  # Operador de asignacion: 
  alt + - `<-` 
  
  # Arrastrar linea: 
  alt + up o alt + down
  
  # ejecutar linea: ctrl + enter
  
  
  
  
  
  
# Para comentar múltiples líneas de código: agrega el numeral al lado izquierdo en cada linea de codigo:
  ctrl/cmd + shift + c
  
# Para movernos en cada pestaña de los scripts:
# Desplazarse por múltiples pestañas de código:
  ctrl + alt + (flechas: izquierda o derecha)
  
  
# Para insertar/crear secciones (títulos):  
  ctrl/cmd + shift + R
  
# Para buscar y reemplazar varias palabras:
  ctrl + F
  
# Dar formato ordenado al código:
  ctrl + A

  

# más atajos: puedes escoger los que más te convengan
  alt +shift + k
  
  
  
  
  
  
  
  
  
  
  
  